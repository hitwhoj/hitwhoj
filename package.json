{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "patch-package && husky install && yarn workspace hitwhoj prisma generate"
  },
  "resolutions": {
    "katex": "^0.15.3",
    "minimist": "^1.2.6",
    "got": "^11.8.5"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
